# AI API key. It can be an OpenAI key (sk-...) OR a Google Gemini / AI Studio key (AIza...).
# The provider is detected automatically by the key format — just paste your key here.
OPENAI_API_KEY=sua_chave_aqui
